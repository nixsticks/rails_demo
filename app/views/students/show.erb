<h1><%= @student.first_name %> <%= @student.last_name %></h1>
<p><%= link_to 'Destroy',  "/students/#{@student.id}" , method: :delete, data: { confirm: 'Are you sure?'} %></p>
<p><%= link_to 'Back', "/students" %></p>